apiVersion: v1
kind: Service
metadata:
  name: health-api-service
spec:
  type: NodePort
  selector:
    app: health-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
